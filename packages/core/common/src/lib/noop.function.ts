/**
 * A function that does nothing.
 * @param _arguments A list of parameters that are not used.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function noop(..._arguments: unknown[]): void {
  // nothing to do
}
